# This app lives under a parent folder that has pnpm-workspace.yaml for other
# projects. Install here as a standalone package so node_modules junctions
# resolve to this directory (not a moved/renamed path).
ignore-workspace=true
